# 智董 (Chairman Agent) 环境配置文件
# 复制此文件为 .env 并填入实际的值

# ==================== OpenRouter + DeepSeek API ====================
# 获取地址: https://openrouter.ai/
OPENROUTER_API_KEY=your_openrouter_api_key_here

# ==================== Open-Notebook 配置 ====================
# 如果使用Docker部署，保持默认值
# 如果使用本地部署，修改为实际的地址
NOTEBOOK_API_URL=http://localhost:5055
NOTEBOOK_API_KEY=your_notebook_api_key_here
NOTEBOOK_WS_URL=ws://localhost:8000/rpc

# ==================== Milvus 向量数据库配置 ====================
# Docker部署时使用 milvus 作为主机名
# 本地部署时使用 localhost
MILVUS_HOST=milvus
MILVUS_PORT=19530

# ==================== Redis 缓存配置 ====================
REDIS_URL=redis://redis:6379/0

# ==================== 应用配置 ====================
DEBUG=false
LOG_LEVEL=INFO
API_PORT=8000
API_HOST=0.0.0.0

# ==================== 存储路径 ====================
DATA_DIR=./data
LOG_DIR=./logs

# ==================== LLM 模型配置 ====================
# 推理模型（用于复杂分析）
MODEL_REASONING=deepseek/deepseek-r1

# 工具调用模型（用于执行工具）
MODEL_TOOLCALL=deepseek/deepseek-v3

# Embedding模型（本地向量化）
MODEL_EMBEDDING=all-MiniLM-L6-v2

# ==================== Agent 配置 ====================
# 知识库检索参数
RETRIEVAL_TOP_K=10
RETRIEVAL_SIMILARITY_THRESHOLD=0.5

# LLM 参数
LLM_TEMPERATURE=0.7
LLM_MAX_TOKENS=4000
LLM_TIMEOUT=120

# ==================== 数据同步配置 ====================
# 同步间隔（秒）
SYNC_INTERVAL_SECONDS=300

# 每次同步的批量大小
SYNC_BATCH_SIZE=50

# ==================== Keycloak 认证配置（可选） ====================
# KEYCLOAK_URL=http://localhost:8080
# KEYCLOAK_REALM=chairman-agent
# KEYCLOAK_CLIENT_ID=api-gateway
# KEYCLOAK_CLIENT_SECRET=your_keycloak_client_secret

# ==================== 数据库配置（可选） ====================
# PostgreSQL 配置（用于Open-Notebook）
# DATABASE_URL=postgresql://notebook_user:notebook_password@postgres:5432/open_notebook

# MongoDB 配置（可选备选方案）
# MONGODB_URL=mongodb://localhost:27017/chairman_agent
